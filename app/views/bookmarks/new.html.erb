
<div class="container">
  <h4>Add a Movie</h4>
  <%= simple_form_for [ @list, @bookmark ] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie, prompt: "Choose a movie" %>
    <%= f.submit "Add" %>
  <% end %>
</div>
